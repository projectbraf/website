import { styled } from '../../../stitches.config';
import { motion } from 'framer-motion';

export const SteamLogoStyle = styled(motion.div,{
    backgroundColor: 'DarkBlue',
    height: '80px',
    boxShadow: '0px 4px 10px Black',
    borderRadius: '0 0 30% 0',
})